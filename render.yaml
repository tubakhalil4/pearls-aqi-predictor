services:
  - type: web
    name: pearls-aqi-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT
    healthCheckPath: /
    autoDeploy: true
